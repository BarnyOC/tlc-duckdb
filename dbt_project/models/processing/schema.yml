# AIModified:2026-06-01T07:29:46Z
version: 2

models:
  - name: prc_trips
    description: Cleaned trips; one row per trip after quality filters.
    columns:
      - name: trip_sk
        description: Surrogate key for a trip.
        tests:
          - unique
          - not_null
      - name: pickup_date
        description: Date of pickup (day truncated).
        tests:
          - not_null
